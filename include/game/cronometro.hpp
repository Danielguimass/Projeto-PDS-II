#ifndef CRONOMETRO_H
#define CRONOMETRO_H

#include <iostream>
#include <iomanip>
#include <ctime>

using namespace std;

    void iniciarCronometro(time_t *tempo);
    void imprimeTempo(time_t *tempo);

#endif